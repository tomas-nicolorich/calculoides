/**
 * Phase 1a placeholder — proves the Next.js App Router shell boots and
 * serves a page with the ported Tailwind 4 theme (`app/globals.css`).
 * Replaced by the real dashboard/groups routing in later phases.
 */
export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-slate-50 dark:bg-slate-950">
      <p className="text-slate-600 dark:text-slate-400 text-sm">
        Calculoides — Next.js shell online.
      </p>
    </main>
  );
}
