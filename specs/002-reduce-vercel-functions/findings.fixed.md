# Findings Fixed Log

> Auto-generated by Critique/Fix-Findings on 2026-05-20

## Summary

- **Total iterations**: 1
- **Findings resolved**: 3
- **Findings deferred**: 0
- **Final status**: CLEAN

## Iteration 1

### Findings Identified
- [Critical] Broken method-based routing for consolidated endpoints — vercel.json, api/src/server.ts
- [Important] Categories SSG Migration is Incomplete — frontend/src/shared/api/client.ts
- [Minor] Benchmark script path mismatch — api/scripts/benchmark.ts

### Fixes Applied
- **Fixed method-based routing**: 
    - Updated `api/src/handlers/groups.ts` to differentiate between `list` (GET) and `create` (POST).
    - Updated `api/src/handlers/transactions.ts` to use a `resource` parameter and differentiate actions based on HTTP methods (GET, POST, DELETE).
    - Updated `api/src/handlers/members.ts` to map DELETE to `remove-member`.
    - Updated `vercel.json` and `api/src/server.ts` to use `resource` query parameters for transactions.
- **Fixed Categories SSG**:
    - Updated `frontend/src/shared/api/client.ts` to consume the static `categories.json` data generated at build time.
    - Added default initializers for `balances` and `totalSpent` to maintain type compatibility.
- **Fixed script paths**:
    - Updated `api/scripts/benchmark.ts` and `api/scripts/generate-categories.ts` to use correct relative paths and support command-line arguments for output.

### Findings Deferred
- None.
